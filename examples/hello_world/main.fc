import i32 puts(string str);

function printHelloWorld(i32 i, i32 count, i32 t)
{
    if (i >= count)
    {
        return 0;
    }

    return tail printHelloWorld(i + 1
        , count
        , puts(@"Hello world!")
        );
}

export i32 main()
{
    return printHelloWorld(0, 5, 0);
}
